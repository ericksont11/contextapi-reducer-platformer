import React from 'react';

const Level = (props) => {
 
  return  (
    <div style={{position:'absolute', left:props.left, width:props.width, height:'5vh', backgroundColor:'brown', top: props.top}}>
    </div>
  ) 
}

export default Level;